#include<stdio.h>
int main()
{
      int n,i=1;
      float a,sum=0;
      printf("enter a number:");
      scanf("%d",&n);
      while(i<=n)
      {
          sum=sum+i;
          i++;
      }
      a=sum/(n+1);
      printf("average=%f",a);
      return 0;
    
}
