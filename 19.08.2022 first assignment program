#include <stdio.h>

int main()
{
 int n,a[100],num,flag=0;
 printf("Enter the size of an array:");
 scanf("%d",&n);
 int i=num;
 for(i=0;i<n;i++)
 {
     scanf("%d",&num);
     if(i==num)
     {
         flag=1;
     }
 }
 if(flag==0)
 printf("UNIQUE\n");
 else
 printf("DUPLICATE");
 

    return 0;
}
